"use client";

import { Input } from "@material-tailwind/react";

import React from "react";

const FusionInput = ({ label, placeholder }) => {
  return <Input label={label} placeholder={placeholder} />;
};

export default FusionInput;
